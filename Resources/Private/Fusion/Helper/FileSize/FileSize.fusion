prototype(NeosRulez.Bootstrap:Helper.FileSize) < prototype(Neos.Fusion:Value) {
    value = ${String.replace(String.toString(Math.round(this.bytes / 1024000, 2)), '.', this.decimalSeparator) + ' MB'}
}
